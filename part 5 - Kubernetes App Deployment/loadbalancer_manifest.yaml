apiVersion: v1
kind: Service
metadata:
  name: app-lb
  namespace: demo
spec:
  type: LoadBalancer
  selector:
    app: my-dep
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP